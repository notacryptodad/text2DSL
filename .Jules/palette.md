# Palette's Journal

## 2024-05-22 - [Accessibility]
**Learning:** The `QueryInput` component was missing crucial ARIA labels, making the primary interaction point difficult for screen reader users.
**Action:** Always verify that form inputs, especially core ones like the main query input, have explicit labels or `aria-label` attributes and are associated with their helper text.
