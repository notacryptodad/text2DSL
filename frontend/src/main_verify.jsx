import React from 'react';
import ReactDOM from 'react-dom/client';
import VerifyComponent from './VerifyComponent';
import './styles/App.css'; // Add the CSS

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <VerifyComponent />
  </React.StrictMode>,
);
