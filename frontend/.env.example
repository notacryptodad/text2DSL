# API Configuration (optional)
# In development, Vite proxy is used automatically
# In production, these can be set to point to your backend

# API Base URL (defaults to same origin in production)
# VITE_API_URL=http://localhost:8000

# WebSocket URL (defaults to same origin in production)
# VITE_WS_URL=ws://localhost:8000
